import styled from "styled-components";

export const RedSeparator = styled.span`
  position: relative;
`;

export const BlueCenterLine = styled.div`
  position: absolute;
  left: 50%;
  top: 0;
  bottom: 0;
  width: 2px;
  background-color: #414141;
`;